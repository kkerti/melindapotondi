---
import { createTranslationFunction } from "../i18n";

const t = await createTranslationFunction(Astro.url);

---

<footer class="w-full bg-green-700 text-white ">
    <div class="container mx-auto mt-auto py-4 px-4 text-center">
    <p class="text-sm"> © {new Date().getFullYear()} - {t("footer.copyright")}</p>
    <p class="text-xs py-2">{t("footer.developer")}</p>
    </div>
</footer>
